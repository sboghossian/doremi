# Notion brain dump — preserved

Source: https://www.notion.so/04b768e8393e48a08ebdec1be4ce8efd
Fetched: 2026-05-19
Notion last-edited: 2025-09-28T11:12:19.385Z

## Files

- **`notion-export-raw.md`** — exact JSON wrapper as returned by Notion MCP (54,019 bytes, single line). Keep for replay/audit.
- **`notion-export.md`** — same content, JSON unwrapped into readable markdown (192 lines). This is the human-readable version.
- **`notion-insights.md`** — structured analysis: vision, timeline, the real 10-point backlog, names, aesthetic, competitors, hardware notes, declared risks, gaps the v1 build must close.

## What's in the dump

The page lived for ~21 months as a vision/branding artifact. Most of it is AI-generated business-plan scaffolding (Half Baked template, scorecard, persona, user journey, market sizing). The substantive raw thinking is in **one toggle called "Process of thinking"** (line 177 of `notion-export.md`), which contains:

1. The original 10-point feature prompt Stephane wrote
2. An early AI-returned pitch under the working name "MusicBox"

Everything else on the page is AI elaboration on top of those two seeds.

## What was NOT in the dump (you have to decide these)

- Sensor/hardware approach (camera vs wearable vs IMU)
- Audio stack (samples vs synth vs MIDI-out)
- App stack
- License
- Latency target
- Accessibility positioning

→ All of these were resolved in the 2026-05-19 scoping session. See `~/.claude/projects/.../memory/doremi-v1-decisions.md`.
